# Claude Code Instructions - ZeroTier Sidecar Project

## 📋 Project Overview

**ZeroTier Sidecar** - Docker-контейнер для маршрутизации трафика через ZeroTier с поддержкой гибридного режима (Gateway mode).

- **Main repo**: `alexbic/zerotier-sidecar` (публичный)
- **Test repo**: `alexbic/zerotier-sidecar-test` (приватный)
- **Docker Hub**: `alexbic/zerotier-sidecar`
- **GHCR**: `ghcr.io/alexbic/zerotier-sidecar`

## 🔄 Development Workflow

### 1. Branching Strategy

- **`main`** - production branch
  - Версии: `v2.x.x` (Gateway с обратной совместимостью)
  - Docker tag: `latest`
  - Auto-deploy после успешных тестов

- **`dev`** / другие ветки - development
  - Версии: `v0.x.x`
  - Docker tag: `dev`
  - Prerelease режим

### 2. Commit Message Conventions

#### Пропуск автотестов (CI):
Используй один из этих маркеров в commit message для пропуска тестов:

```
[skip ci]    - пропустить CI полностью
[skip-ci]    - то же самое
[deploy]     - пропустить тесты, но разрешить deploy
[no-test]    - пропустить тесты
```

**Примеры:**
```bash
git commit -m "fix: redirect logs to stdout [skip ci]"
git commit -m "docs: update README [no-test]"
git commit -m "[deploy] hotfix: critical bug fix"
```

#### Когда использовать skip маркеры:
- ✅ Изменения только в документации (README, docs/)
- ✅ Изменения только в workflows (.github/)
- ✅ Мелкие исправления без изменения логики
- ✅ Hotfix который нужно задеплоить немедленно
- ❌ Изменения в `Dockerfile` или `start-sidecar.sh` (всегда тестируй!)

### 3. Автоматический CI/CD процесс

#### При push в `main` БЕЗ skip маркера:

1. **CI Workflow** (`ci.yml`) запускается
2. Проверяет commit message на skip маркеры
3. Если нет маркера → триггерит тесты в приватном репо `zerotier-sidecar-test`
4. После успешных тестов → auto-deploy (repository_dispatch: deploy-approved)

#### При push в `main` СО skip маркером:

1. CI пропускается
2. Можно вручную запустить deploy через GitHub Actions

### 4. Deploy Workflow (`deploy.yml`)

#### Автоматические проверки:

**Проверка нужности Docker build:**
- Если изменились ТОЛЬКО: `.github/`, `README*`, `.gitignore` → build пропускается
- Если изменились: `Dockerfile`, `start-sidecar.sh` → build запускается
- Manual trigger → всегда build

#### Версионирование:

**Автоматический push (без manual trigger):**
- Всегда: `patch` increment
- Пример: `v2.1.4` → `v2.1.5`

**Manual trigger через Actions:**
- `patch` - инкремент патч версии (v2.1.4 → v2.1.5)
- `minor` - инкремент минор версии (v2.1.4 → v2.2.0) + создает GitHub Release
- `custom` - кастомная версия (для rebuild существующей версии)

#### GitHub Release:
- Создается ТОЛЬКО для `minor` версий при manual trigger
- `patch` версии НЕ создают release (только Docker tags)

### 5. Типичные сценарии работы

#### Сценарий A: Обычная разработка (с тестами)

```bash
# 1. Внеси изменения в код
git add start-sidecar.sh
git commit -m "fix: redirect connection logs to container stdout

Problem: AWK process output was not visible in docker logs
Solution: Pipe through 'while read' to ensure stdout redirection

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"

# 2. Push (автоматически запустятся тесты)
git push origin main

# 3. Дождись прохождения тестов в zerotier-sidecar-test
# 4. После успешных тестов → автоматический deploy
```

#### Сценарий B: Документация или minor fixes (пропуск тестов)

```bash
git add README.md
git commit -m "docs: update deployment instructions [skip ci]"
git push origin main

# Тесты НЕ запустятся
# Можно потом вручную задеплоить через Actions если нужно
```

#### Сценарий C: Hotfix (срочное исправление)

```bash
git add start-sidecar.sh
git commit -m "[deploy] hotfix: fix critical logging bug

Urgent fix for production issue.
Tests will be run after deploy.

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"

git push origin main

# 1. CI пропустится ([deploy] маркер)
# 2. Вручную запусти Deploy workflow:
#    Actions → Deploy Production Release → Run workflow
```

#### Сценарий D: Minor release с changelog

```bash
# 1. Закоммить изменения
git add .
git commit -m "feat: add advanced logging system with multiple modes

✨ New Features:
- Simple logging mode for production
- Full logging mode for debugging
- Automatic log rotation
- Connection tracking with IP mapping

🐛 Bug Fixes:
- Fixed log output redirection to stdout
- Fixed service monitor false positives

📚 Documentation:
- Added logging configuration guide
- Updated README with new examples

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"

git push origin main

# 2. После успешных тестов → вручную создать minor release:
#    Actions → Deploy Production Release → Run workflow
#    - version_type: minor
#    - release_notes: (оставить пустым, будет использован commit message)
```

#### Сценарий E: Rebuild существующей версии

```bash
# Если нужно пересобрать v2.1.5 без изменения версии:
# Actions → Deploy Production Release → Run workflow
#   - version_type: custom
#   - custom_version: v2.1.5
#   - skip_tests: true (если тесты не нужны)
#   - no_cache: true (если нужна полная пересборка)
```

## 🧪 Testing

Тесты запускаются в приватном репо `alexbic/zerotier-sidecar-test`:
- Gateway runner: `vps-gateway-test`
- Client runner: `nas-client-test`
- Полный набор тестов: connection, routing, hybrid mode, etc.

## 🐳 Docker Build

```bash
# Local build для тестирования
docker build -t zerotier-sidecar:test .

# Build with cache
docker buildx build --platform linux/amd64 -t alexbic/zerotier-sidecar:test .

# Build without cache (полная пересборка)
docker buildx build --platform linux/amd64 --no-cache -t alexbic/zerotier-sidecar:test .
```

## 📁 Важные файлы

- `start-sidecar.sh` - главный скрипт контейнера (1350 строк)
- `Dockerfile` - описание Docker образа
- `.github/workflows/ci.yml` - автотесты trigger
- `.github/workflows/deploy.yml` - deploy и release workflow
- `docker-compose.yml` - пример использования
- `.env.example` - пример конфигурации

## ⚠️ Важные правила

1. **ВСЕГДА тестируй** изменения в `Dockerfile` и `start-sidecar.sh`
2. **НЕ используй** `[skip ci]` для критичных изменений
3. **Используй conventional commits** для ясности
4. **Minor releases** создавай вручную с подробным changelog
5. **Patch releases** происходят автоматически при push
6. **Проверяй** что Docker build не сломался перед push

## 🔧 Текущая задача (контекст для Claude)

**Проблема:** AWK процесс для парсинга логов запускается в background (&), но его stdout не перенаправлен в основной процесс контейнера, поэтому логи не видны в `docker logs`.

**Решение:** Добавлен pipe через `while read` loop с explicit echo для обеспечения редиректа в container stdout.

**Файлы:**
- `start-sidecar.sh` строки 1294, 1302

**Следующие шаги:**
1. ✅ Commit с маркером (так как это fix, но можно пропустить тесты для быстрого deploy)
2. Push в main
3. Тесты (или skip)
4. Deploy (auto или manual)

## 🚀 Quick Commands

```bash
# Commit с пропуском тестов
git commit -m "fix: your message [skip ci]"

# Commit с тестами
git commit -m "fix: your message"

# Push
git push origin main

# Manual deploy trigger (через GitHub Actions UI)
# Actions → Deploy Production Release → Run workflow
```

---

*Последнее обновление: 2025-11-13*
*Автор: Claude Code*
